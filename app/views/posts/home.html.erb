<% provide(:title, "Home") %>
<div class = "hero-unit" >

	<div class = "row">
	
	      <% if @posts.any? %>
	        <div id = "postCount">Found <%= pluralize(@posts.count, 'post')%></div>
	          <ol class="posts">
	            <%= render @posts %>
	          </ol>
	     
	        <%= will_paginate @posts %>
	     <% end %>
	 
	</div>

</div>

		<img alt = "dexileos" src= <%= asset_path('dexileos.png') %> id="dexileos" />

